# Spring Version 5.3.21
# Java SDK 17.0.4
# Java 17

#--------------------------------------------------------------------------------------------------------------#
#-------------------------- NE PAS UTILISER D'ACCENTS FRANCAIS DANS CE FICHIER --------------------------------#
#--------------------------------------------------------------------------------------------------------------#

logging.level.web = debug
logging.level.sql = debug
logging.level.springframework=debug

# Activate SQL in Run Terminal (usefull for debug)
# spring.jpa.properties.hibernate.show_sql=true

# new Date() doesn't generate a date stamp but ISO Date
spring.jackson.serialization.write_dates_as_timestamps=false

# MySQL
# Si un changement est fait dans les entites
# So the list of possible options are,
#    validate: validate the schema, makes no changes to the database.
#    update: update the schema.
#    create: creates the schema, destroying previous data.
#    create-drop: drop the schema when the SessionFactory is closed explicitly, typically when the application is stopped.
#    none: does nothing with the schema, makes no changes to the database
spring.jpa.hibernate.ddl-auto=none

# Base de donnee en Local
# spring.datasource.url=jdbc:mysql://localhost:3306/spring_colomb

# Base de donnee
# Prod : creer key/value sur Azure "new Connection String" + Type: CUSTOM!
# Local: creer key/value sur Windows comme variable environnement + Restart PC
spring.datasource.url=${CUSTOMCONNSTR_AZURE_DB_URL_COLOMB}
spring.datasource.username=${CUSTOMCONNSTR_AZURE_DB_ADMIN_COLOMB}
spring.datasource.password=${CUSTOMCONNSTR_AZURE_DB_PASS_COLOMB}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQL5InnoDBDialect

# App Properties - Ne rien renseigner ici pour jwtSecret
# Prod : creer key/value sur Azure "new Application settings" avec la meme cle "aston.colomb.app.jwtSecret" + le secret
# Local: creer key/value sur Windows comme variable environnement + Restart PC
aston.colomb.app.jwtSecret=
aston.colomb.app.jwtExpirationMs= 86400000

#--------------------------------------------------------------------------------------------------------------#
#-------------------------- NE PAS UTILISER D'ACCENTS FRANCAIS DANS CE FICHIER --------------------------------#
#--------------------------------------------------------------------------------------------------------------#